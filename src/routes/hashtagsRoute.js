import { Router } from "express";

const router = Router();

// router.get("/trending", getTrendingHashtags); // add validateUserById
// router.get("/:hashtag/posts", validateHashtag, getHashtagPosts); // add validateUserById

export default router;
